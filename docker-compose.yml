services:
  portfolio:
    build: .
    container_name: react-portfolio
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - node_modules_data:/app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true

volumes:
  node_modules_data:
